import { RegisterPageContent } from "@/domains/auth/components/register-page-content";

export const metadata = {
  title: "Criar conta",
};

export default function RegisterPage() {
  return <RegisterPageContent />;
}
