import { LoginPageContent } from "@/domains/auth/components/login-page-content";

export const metadata = {
  title: "Entrar",
};

export default function LoginPage() {
  return <LoginPageContent />;
}
